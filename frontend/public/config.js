// Runtime configuration — for LOCAL DEVELOPMENT only.
// In Docker, this file is generated at container startup by docker-entrypoint.sh
// with the real VITE_API_BASE / VITE_WS_BASE values.
//
// In dev mode (npm run dev), leave these empty so the fallback chain in
// src/config.js reaches import.meta.env.VITE_* (set in .env).
window.__RUNTIME_CONFIG__ = {
  VITE_API_BASE: "",
  VITE_WS_BASE: ""
};

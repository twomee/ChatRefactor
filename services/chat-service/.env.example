# Chat Service Configuration
# Copy to .env and fill in real values.

APP_ENV=dev
PORT=8003
LOG_LEVEL=info

# PostgreSQL — the chat-service owns chatbox_chat
DATABASE_URL=postgresql://chatbox:chatbox_pass@localhost:5432/chatbox_chat

# Redis — used for WebSocket pub/sub (future horizontal scaling)
REDIS_URL=redis://localhost:6379/0

# Kafka — message bus for persistence and cross-service events
KAFKA_BOOTSTRAP_SERVERS=localhost:29092

# Shared JWT secret — must match auth-service
SECRET_KEY=change-this-in-production

# WebSocket allowed origins — comma-separated list for production CORS.
# Leave empty or omit in dev mode (all origins allowed).
# ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com

# Downstream service URLs
AUTH_SERVICE_URL=http://localhost:8001
MESSAGE_SERVICE_URL=http://localhost:8004

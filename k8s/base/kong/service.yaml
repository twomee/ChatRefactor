apiVersion: v1
kind: Service
metadata:
  name: kong
  namespace: chatbox
  labels:
    app.kubernetes.io/name: kong
    app.kubernetes.io/part-of: chatbox
    app.kubernetes.io/component: gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kong
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
